class Video {
  constructor(id, title, description, thumbnailUrl, videoUrl) {
    this.id = id;
    this.title = title;
    this.thumbnailUrl = thumbnailUrl;
    this.description = description;
    this.videoUrl = videoUrl;
  }
}

export default Video;
