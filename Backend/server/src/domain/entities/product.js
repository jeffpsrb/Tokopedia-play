class Product {
  constructor(id, title, price, imageUrl, productUrl) {
    this.id = id;
    this.title = title;
    this.price = price;
    this.imageUrl = imageUrl;
    this.productUrl = productUrl;
  }
}

export default Product;
